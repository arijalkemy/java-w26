# "prod" suffix properties file. Scopes name samples: "prod", "web-prod", "api-prod", etc.
# Overrides application.yml default properties
spring:
  datasource:
    host: DB_MYSQL_DESAENV09_FRESHPRODR_FRESHPRODR_ENDPOINT
    db: freshprodr
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: freshprodr_WPROD
    password: DB_MYSQL_DESAENV09_FRESHPRODR_FRESHPRODR_WPROD
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: false
    hibernate:
      ddl-auto: update