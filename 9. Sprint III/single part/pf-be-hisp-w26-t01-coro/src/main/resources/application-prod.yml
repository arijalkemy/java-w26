# "prod" suffix properties file. Scopes name samples: "prod", "web-prod", "api-prod", etc.
# Overrides application.yml default properties
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    db: CoroSp3DB
    username: CoroSp3DB_WPROD
    password: DB_MYSQL_DESAENV09_CoroSp3DB_CoroSp3DB_WPROD
    host: DB_MYSQL_DESAENV09_CoroSp3DB_CoroSp3DB_ENDPOINT
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: true
    generate-ddl: false
    hibernate:
      ddl-auto: none
