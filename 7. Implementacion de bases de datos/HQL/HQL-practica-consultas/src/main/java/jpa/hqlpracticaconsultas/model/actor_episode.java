package jpa.hqlpracticaconsultas.model;

import jakarta.persistence.*;

@Entity
@Table(name = "actor_episode")
public class actor_episode {
    @Id
    @GeneratedValue(GenerationType.SEQUENCE)
    private Long id;
    private cerated_at
}
